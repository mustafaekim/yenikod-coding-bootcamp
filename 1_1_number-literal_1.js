// You can run this file with `node filename.js` where you will replace filename with the real filename

// Below is a code
// It does not do much. The only code you see is a "number literal"
// "Number literal" is a representation of a number value
1

// Even though the code does not do or produce anything, the point is that this code works without any error.