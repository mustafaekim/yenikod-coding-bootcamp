var foo, bar, zar;

var name = "Mustafa", age = 35;

var firstName, lastName = "Ekim", school = "Mkool"; // That's a single statement which executes multiple variable declarations & assignment

//console.log(foo, bar, zar, name, age, firstName, lastName, school);